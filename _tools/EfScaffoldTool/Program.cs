// This project is a design-time-only host for the "dotnet ef dbcontext scaffold" command line tool
// (see Generate-EfMigrations.ps1 in the repo root). It is never meant to be run directly.
Console.WriteLine("EfScaffoldTool is a design-time-only host for \"dotnet ef dbcontext scaffold\" and is not meant to be run directly. Use Generate-EfMigrations.ps1 instead.");
